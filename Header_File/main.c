#include "../Common/headers.h"
#include <stdio.h> // system header 
#include "add_util.h"   // user-defined header
#include "add_util.h"
#include "add_util.h"

int main() {

    int a = 10;
    int b = 20;

    printf("Addition of two numbers: %d", add(a, b));
    return 0;
}